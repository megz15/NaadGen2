export type Raga = {
    vikrit: string[]
    vikrit_shuddha: string[]
    varjya: string[]
    vadi: string
    samvadi: string
}

export type Taal = {
    matra: number,
    tali: number[],
    khali: number[],
}